# CI 工作流（已禁用）

# 注意：这是模板项目，用户需要根据实际情况配置 package.json 后再启用 CI
# 启用方法：删除以下 'if: false' 条件

name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop]
  workflow_dispatch:  # 允许手动触发

jobs:
  # 已禁用：取消注释以启用
  # lint:
  #   name: Lint
  #   if: false  # 模板项目默认禁用
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Run linter
  #       run: npm run lint
  #
  # typecheck:
  #   name: Type Check
  #   if: false  # 模板项目默认禁用
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Type check
  #       run: npm run typecheck
  #
  # test:
  #   name: Test
  #   if: false  # 模板项目默认禁用
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Run tests
  #       run: npm test
  #
  # build:
  #   name: Build
  #   if: false  # 模板项目默认禁用
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'
  #     - name: Install dependencies
  #       run: npm ci
  #     - name: Build
  #       run: npm run build

  # 说明任务（用于显示 CI 状态）
  placeholder:
    name: Template Notice
    runs-on: ubuntu-latest
    steps:
      - name: Template Notice
        run: |
          echo "⚠️  This is a template project"
          echo "CI jobs are disabled by default."
          echo "After configuring package.json, uncomment the CI jobs in .github/workflows/ci.yml"
